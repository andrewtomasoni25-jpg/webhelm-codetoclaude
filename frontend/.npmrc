# Accept peer-dep mismatches from shadcn/ui components.
# Specifically react-day-picker@8 wants date-fns@^2 || ^3 but we run
# date-fns@4. npm ci bails without this; npm install tolerates it but
# Vercel uses ci. Applies equally to local installs.
legacy-peer-deps=true
